apiVersion: v1
kind: Service
metadata:
  name: lineage-manager
  namespace: self-scheduling-dev1
  labels:
    app: lineage-manager
spec:
  type: NodePort
  ports:
  - port: 5003
    targetPort: 5003
    protocol: TCP
    name: http
  selector:
    app: lineage-manager
