---
description: sync latest code from origin develop
---

1. Checkout the develop branch and pull the latest changes from the origin.
// turbo
2. Run the sync command from the root Makefile.
```bash
make sync
```
