apiVersion: v1
kind: Service
metadata:
  name: {{ include "lineage-manager-v2.fullname" . }}
  labels:
    {{- include "lineage-manager-v2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "lineage-manager-v2.selectorLabels" . | nindent 4 }}
