# Local Development Environment Variables
# This file is used when running `make run` locally
# Docker Compose uses its own environment variables defined in docker-compose.yml

# Database Configuration (for local development)
# Database Settings
DB_DRIVER=mysql+pymysql
DB_HOST=172.21.160.1
DB_PORT=33306
DB_USER=ss_admin
DB_PASSWORD=admin_password
DB_NAME=lineage_manager


# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
