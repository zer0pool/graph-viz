# Report: SSO Internal Deployment Readiness

We have successfully migrated the legacy SSO logic into the Shell MFE. The system is now ready for internal deployment and verification.

## 1. Implemented Features

### PKCE-Based OIDC Flow

The Shell now handles the full security handshake with OIDC providers (Google and others):

- **Verifier/Challenge Generation**: Using Web Crypto API for secure state management.
- **Config-Driven**: Dynamic fetching of Auth server metadata.
- **Authorization Code Exchange**: Integrated with the existing backend `/auth/exchange` API.

### Phase-Based Logging (Extreme Observability)

In-Net 환경에서 인증이 어디서 막히는지 즉시 파악할 수 있도록 로그를 대폭 강화했습니다:

- `[Auth][Phase:Initialization]`: 앱 초기화 및 세션 복구 단계
- `[Auth][Phase:LoginStart]`: PKCE 생성 및 리다이렉트 발생 단계
- `[Auth][Phase:Callback]`: 백엔드와 토큰 교환 단계
- `[Auth][Phase:Fetch]`: 모든 API 호출 시 토큰 주입 여부 기록

## 2. Docker & Build (이미지 최적화)

Shell MFE 배포를 위해 다음 파일들을 생성했습니다:

- **[Dockerfile](file:///home/darkwing/src/lineage_platform/apps/admin_console/shell/Dockerfile)**: Multi-stage 빌드를 통해 최종 이미지를 `nginx:alpine` 기반의 매우 작은 사이즈(~30MB 미만)로 구성했습니다.
- **[entrypoint.sh](file:///home/darkwing/src/lineage_platform/apps/admin_console/shell/entrypoint.sh)**: 컨테이너 실행 시 환경 변수 주입 등 런타임 설정을 처리할 수 있는 통로를 마련했습니다.

### UI & Session Integration

- **Navbar**: Displaying real user profile (name, avatar, email).
- **Session Persistence**: Backward compatible with `lm.tokens` and `lm.user` storage keys.
- **Auto-Attach**: All API calls from the Shell and MFEs automatically include the `Authorization: Bearer <token>` and `X-User` headers.

## 2. Next Steps for Internal Deployment

1. **Deploy Build**: Copy the updated `apps/admin_console/shell` directory to the internal environment.
2. **Configure Backend**: Ensure the internal OIDC provider URL is set in the backend environment variables or through the `/api/v1/auth/config` response.
3. **Verify Redirects**: Confirm that the `redirect_uri` in the provider matches the `/authorized` path of the Shell.

## 3. Verification Checklist

- [ ] Login button initiates redirect.
- [ ] Successful return to `/authorized` updates the Shell state.
- [ ] Console logs confirm successful token exchange.
- [ ] Dashboard shows user profile correctly.

---

_Report generated by Antigravity on 2026-01-10_
