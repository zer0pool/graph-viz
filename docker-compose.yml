version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./apps:/app/src
    environment:
      - PORT=8000
    restart: unless-stopped